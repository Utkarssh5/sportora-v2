'use client';

import { useState } from 'react';
import Link from 'next/link';
import { Trophy, Users, Radio, User, PlusCircle, ShoppingBag, Sun, Moon } from 'lucide-react';
import AuthModal from './AuthModal';
import { useTheme } from '../context/ThemeContext';

export default function Navbar() {
  const [isAuthOpen, setIsAuthOpen] = useState(false);
  const { theme, toggleTheme } = useTheme();

  return (
    <>
      <nav className="fixed top-0 left-0 right-0 z-40 px-6 py-4 backdrop-blur-xl border-b transition-colors duration-300" style={{ backgroundColor: 'var(--nav-bg)', borderColor: 'var(--card-border)' }}>
        <div className="max-w-7xl mx-auto flex items-center justify-between">
          
          {/* Logo */}
          <Link href="/" className="flex items-center gap-2 group">
            <div className="w-9 h-9 rounded-xl bg-[#00FF66] flex items-center justify-center text-black font-black text-lg group-hover:scale-105 transition-transform">
              S
            </div>
            <span className="font-black text-xl tracking-tight group-hover:text-[#00FF66] transition-colors">
              SPORTORA<span className="text-[#00FF66]">.V2</span>
            </span>
          </Link>

          {/* Nav Links */}
          <div className="hidden md:flex items-center gap-8 text-xs font-bold uppercase tracking-wider">
            <Link href="/" className="hover:text-[#00FF66] transition-colors flex items-center gap-1.5">
              <Trophy className="w-4 h-4 text-[#00FF66]" /> Arena Hub
            </Link>
            <Link href="/organizer" className="hover:text-emerald-500 transition-colors flex items-center gap-1.5">
              <PlusCircle className="w-4 h-4 text-emerald-500" /> Host Event
            </Link>
            <Link href="/crew" className="hover:text-emerald-500 transition-colors flex items-center gap-1.5">
              <Users className="w-4 h-4 text-emerald-500" /> Ground Crew
            </Link>
            <Link href="/store" className="hover:text-amber-500 transition-colors flex items-center gap-1.5">
              <ShoppingBag className="w-4 h-4 text-amber-500" /> Sponsor Store
            </Link>
            <Link href="/profile" className="hover:text-[#00FF66] transition-colors flex items-center gap-1.5">
              <User className="w-4 h-4 text-[#00FF66]" /> My Profile
            </Link>
          </div>

          <div className="flex items-center gap-3">
            {/* Theme Toggle Button */}
            <button
              onClick={toggleTheme}
              className="p-2.5 rounded-full bg-white/10 hover:bg-white/20 border border-white/10 transition-all text-amber-400 hover:scale-110"
              title="Switch Theme View"
            >
              {theme === 'dark' ? <Sun className="w-4 h-4 text-amber-400" /> : <Moon className="w-4 h-4 text-indigo-600" />}
            </button>

            {/* Action Login Button */}
            <button
              onClick={() => setIsAuthOpen(true)}
              className="bg-white/10 hover:bg-[#00FF66] hover:text-black font-extrabold text-xs px-5 py-2.5 rounded-full border border-white/10 hover:border-[#00FF66] transition-all flex items-center gap-2"
            >
              <User className="w-4 h-4" /> LOGIN / SIGNUP
            </button>
          </div>

        </div>
      </nav>

      <AuthModal isOpen={isAuthOpen} onClose={() => setIsAuthOpen(false)} />
    </>
  );
}
