version: '3.8'

services:
  sportora-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
